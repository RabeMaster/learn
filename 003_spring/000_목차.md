# 003_spring

스프링의 트랜잭션 처리 정리.

## 글

| 글 | 다루는 것 | 상태 |
| --- | --- | --- |
| [001_transactional-동작원리.md](001_transactional-동작원리.md) | 프록시, 자기호출 문제, 전파(propagation), 롤백 규칙 | 시작 전 |
| [002_트랜잭션과-커넥션풀.md](002_트랜잭션과-커넥션풀.md) | 긴 트랜잭션이 커넥션 풀을 고갈시키는 과정 | 시작 전 |

상태는 `시작 전` -> `작업 중` -> `러프` -> `완성` 순으로 바꾼다.

## 글 뼈대

```markdown
# 제목

## 이 글에서 다루는 것

## 참고
```

가운데 본문은 글마다 자유롭게 구성한다.
