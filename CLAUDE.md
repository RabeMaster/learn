# CLAUDE.md

이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 참고하는 안내서입니다.

## 저장소 개요

개인 공부용 저장소. 주제별로 공부 노트, 블로그 글(Markdown), 실습 코드를 모은다. Obsidian 볼트이며(`.obsidian/`은 git 제외), 글은 한국어 플레인 Markdown으로 쓴다(`[[위키링크]]` 금지). 저장소 공통 빌드/테스트 도구는 없음. 실습 프로젝트는 각자의 도구를 먼저 확인할 것.

## 폴더/이름 규칙

- 주제 폴더: `NNN_주제/` (세 자리, 0으로 채움). 예: `001_java/`, `002_gc/`. 이 번호는 저장소 전체 기준으로 주제 폴더가 생긴 순서(전역 시퀀스)다.
- 글: `NNN_제목.md`. 이미지: 폴더별 `images/`. 이 번호는 폴더 안에서 글이 쓰인 순서(로컬 시퀀스)이며, 폴더 번호와는 층위가 다르다.
- 각 주제 폴더에 `000_목차.md`를 둔다. 그 폴더의 글 목록, 진행 상태, 글 템플릿을 담는 목차 파일이다.
- 폴더는 "글 1개"가 아니라 "주제 영역" 단위다. 같은 주제 안의 세부 시리즈(예: java 폴더 안의 "자바 원리", "Java8 vs 21 차이")는 새 폴더를 만들지 않고 같은 폴더에 글 번호만 늘려 쌓는다. 주제 자체가 확실히 다르면(예: java vs spring) 새 주제 폴더를 만든다.
- 실습 프로젝트는 주제 폴더 안에 둔다. 프로젝트가 여러 개면 주제 폴더 안에 프로젝트별 하위 폴더를 둔다(예: `001_java/java8-vs-21-project/`). 커밋 전 빌드 산출물(`target/`, `build/`, `.gradle/`, `bin/`)을 `.gitignore`에 추가.

## 글 규칙

- 최소 뼈대는 제목(H1), `## 이 글에서 다루는 것`, `## 참고` 세 가지뿐이다. 가운데 본문 구성은 글마다 자유이며 틀을 강제하지 않는다.
- 글의 진행 상태는 글 파일이 아니라 그 폴더 `000_목차.md`의 상태 칸에서만 관리한다(`시작 전` -> `작업 중` -> `러프` -> `완성`).
- `## 참고`에는 근거로 삼은 공식 문서 링크를 적는다.
- 도표, 순서도, 타임라인은 이미지 대신 Mermaid 코드블록을 먼저 쓴다. GitHub, Obsidian, 티스토리 모두 렌더링한다. 이미지는 Mermaid로 못 그리는 것(구조도, 스크린샷)에만 쓴다.
- 이미지는 주제 폴더의 `images/` 하나에 모으고, 파일명 앞에 글 번호를 붙인다(예: `002_격리수준-표.png`). 글마다 폴더를 따로 만들지 않는다. 여러 글에서 같은 이미지를 쓰면 복사해서 각자 이름으로 둔다.
- 퍼온 이미지는 바로 아래 줄에 `> 출처: [사이트](URL)` 형식으로 출처를 적는다. 공개 저장소라 재배포가 되므로, 라이선스가 불분명한 이미지는 넣지 말고 링크만 건다.
- 다른 글은 본문 흐름 안에서 링크한다. 링크 텍스트는 "여기" 대신 글 제목을 쓴다(블로그로 옮길 때 URL만 갈아끼우면 되도록).
- 인용 블록처럼 한 문단 안에서 줄을 나눌 때는 줄 끝에 공백 2개를 넣는다. GitHub의 `.md` 파일은 단순 엔터를 줄바꿈으로 렌더링하지 않는다(티스토리와 Obsidian은 되므로 놓치기 쉽다). `.editorconfig`가 `*.md`의 줄 끝 공백을 유지하도록 되어 있다.
- 강조(`**`)를 닫을 때 바로 앞이 닫는 괄호면 GitHub에서 별표가 그대로 노출된다. 괄호는 강조 밖으로 뺀다(`**장부(테이블 파일)**는` -> `**장부**(테이블 파일)는`).
- 화살표는 `->`, 대시는 `-`를 쓴다. 유니코드 화살표와 대시(`→`, `—`, `–`), 가운뎃점(`·`)은 쓰지 않는다. 나열은 쉼표로 한다.
- 위 두 항목(강조와 괄호, 특수문자)은 `.githooks/pre-commit`이 커밋할 때 검사해서 거부한다. 인라인 코드(백틱 안)는 검사하지 않는다.

## 커밋/PR 규칙

- 커밋과 PR에 `Co-Authored-By:`(누구든)와 AI 귀속 문구(`Generated with Claude Code`, `Claude-Session`)를 넣지 않는다. `.githooks/commit-msg`가 거부하며 `.claude/settings.json`이 attribution을 끈다.
- 커밋 제목은 `type(scope): 설명` 형식. type = feat/fix/docs/refactor/style/test/chore, scope는 주제 폴더 이름(생략 가능). merge 커밋은 예외. `.githooks/commit-msg`가 강제.
- scope에는 주제 폴더까지만 쓴다. 글 번호와 주제는 설명에 넣는다(예: `docs(001_db): 005 비관적 락과 낙관적 락 정리`). 여러 폴더에 걸치면 scope를 생략한다.
- 새로 클론하면 한 번: `git config core.hooksPath .githooks`.
- 커밋과 푸시는 요청받을 때만. 공개 푸시는 먼저 확인.

## 비밀정보

공개 저장소. 비밀번호, API 키, `.env` 등 민감정보는 커밋하지 않는다.

## 코드 스타일 (`.editorconfig`)

- UTF-8, LF, 파일 끝 개행. `*.md`는 줄 끝 공백 유지, 그 외는 제거.
- Java: 2칸 들여쓰기 (Google Java Style. 4칸으로 바꾸지 말 것).
- Obsidian은 `.editorconfig`를 적용하지 않음(IDE와 Claude 편집에만 적용).

## 작업 방식

- 사용자 글은 문체를 유지하며 가볍게 교정한다(틀림, 어색함, 위험만 짚기). 요청 없이 전면 재작성 금지.
- 기술 설명은 기억에 의존하지 않는다. 최신 공식 문서로 확인하고 교차검증한 뒤 답하고, 근거 링크를 함께 준다.
- 제품과 버전마다 동작이 다른 내용은 어느 제품, 어느 버전 기준인지 반드시 밝힌다(예: "PostgreSQL 16 기준", "MySQL 8.0 InnoDB 기준"). 다른 제품에서 다르게 동작하면 그 차이도 짚는다. 사용자가 스택을 명시하지 않았고 답이 갈리는 주제면 먼저 묻는다.

## 소통

항상 한국어로, 쉬운 말로. 문서도 한국어. 어려운 용어는 풀어서 설명.
